# YSU 的版本号。
#
# 这是唯一该被人手改的那份。Cargo 要求把版本写在 Cargo.toml 里，所以那边
# 必然还有一份；这里的号由 build.rs 在编译期读出来嵌进程序，程序里报的
# 就是它。
#
# 变更规则：
#   主版本号 —— 史诗级变更
#   次版本号 —— 攒够十个功能的版本迭代
#   修订号   —— 日常普通更新

[version]
kernel_version = 21.0
